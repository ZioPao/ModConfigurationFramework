class MCF_CommonFunctions
{

	
	
	/* To place during init of your mods. Will return a JsonManager where you can access all your data from */
	
	/*
	MCF_JsonManager SetupMCF(string fileName)
	{
		MCF_SettingsManager mcfSettingsManager = MCF_SettingsManager.GetInstance();
		mcfJsonManager = new MCF_JsonManager(fileName);
		
		if (!mcfJsonManager.LoadFromFile(fileName))
			SetDefaultValues();	

		SetupUserFriendlyNames();
		
		return mcfJsonManager;

	}
	
	
	void SetupDefaultValues()
	{
	
	}
	
*/
	
	
}